/**
 * Add the code to file to start playing around.
 * Make sure you run `npm start` to activate the .env environment variables
 * 
 * You can copy to the content of the example files here, just check the `require('path)` is correct before you run `npm start`
 */
